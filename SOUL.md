# SOUL

## 铁律：技术问题必须调用相关技能
以后每次解决技术问题时，都必须主动检查并调用相关技能来辅助完成任务。凡是涉及前端设计、后端设计、工程评测、框架实践、自动化、研究、测试、文档、架构、代码审查、部署、数据处理、Agent 工程等技术方向的问题，都要优先匹配并使用当前可用技能，而不是只凭通用回答处理。

## 铁律：技能来源不限于 ECC
必须调用的技能不一定是 ECC 技能，而是所有当前环境中能用得上的技能。包括但不限于 ECC skills、系统 skills、用户安装 skills、superpower 相关规则、插件内置 skills、项目内技能规则，以及技能文件中进一步指向的可用规则。选择技能时以问题匹配度和实际帮助为准，不以技能来源为准。

## 铁律：汇报已调用技能
每次调用技能辅助完成任务后，都必须在工作汇报或最终回复中列出本轮调用了哪些技能，以及每个技能用于辅助解决什么问题。

## 铁律：技能调用必须随机应变
每一轮调用哪些技能不能在任务刚开始时机械固定。处理过程中一旦遇到新的问题类型、风险点或不确定领域，必须重新判断并临时调用对应技能；汇报中的技能列表要反映本轮实际解决的问题，而不是沿用上一轮模板。

## 铁律：不得擅自变更前端设计和按钮
以后处理本项目时，不准擅自改动前端视觉设计、布局风格、按钮样式、按钮文案、交互位置、主题配色或已有 UI 结构。只允许完善功能、修复数据流、补齐接口、修复运行错误、增强稳定性和测试。除非用户明确要求调整设计，否则任何前端改动都必须保持现有设计和按钮表现不变。

## 铁律：后台信号扫描必须按 15 分钟窗口执行
本项目的核心扫描推送逻辑必须固定为：服务器只在每个 15 分钟 K 线边界触发扫描窗口，即 00、15、30、45 分钟。由于行情和服务器可能存在延迟，边界开始后的 1 分钟内持续扫描所有启用推送的监控标的；扫描到一个真实信号就立即向监控邮箱发送一封邮件，扫描到多少个真实信号就发送多少封，不能等整批结束再统一发送。1 分钟窗口结束后必须停止扫描，等待下一个 15 分钟边界。不得擅自改成每 60 秒、每 120 秒或其他固定间隔轮询扫描。
